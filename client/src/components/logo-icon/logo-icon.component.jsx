import React from "react";
import { Container, SVG } from "./logo-icon.styles";

const LogoIcon = () => {
  return (
    <Container to="/">
      <SVG></SVG>
    </Container>
  );
};

export default LogoIcon;
