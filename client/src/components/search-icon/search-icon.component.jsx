import React from "react";
import { Container, SVG } from "./search-icon.styles";

const SearchIcon = () => {
  return (
    <Container>
      <SVG></SVG>
    </Container>
  );
};

export default SearchIcon;
