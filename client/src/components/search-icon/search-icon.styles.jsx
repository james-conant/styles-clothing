import styled from "styled-components";

import { ReactComponent as SearchIcon } from "../../assets/magnifier.svg";

export const Container = styled.div`
  width: 32px;
  cursor: pointer;
`;

export const SVG = styled(SearchIcon)``;
